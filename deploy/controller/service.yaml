apiVersion: v1
kind: Service
metadata:
  name: queue-aware-vgpu-controller
  namespace: queue-aware-vgpu
  labels:
    app.kubernetes.io/name: queue-aware-vgpu-controller
spec:
  selector:
    app.kubernetes.io/name: queue-aware-vgpu-controller
  ports:
    - name: metrics
      port: 8080
      targetPort: metrics
